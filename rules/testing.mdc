---
description: 测试编写规范
alwaysApply: false
---

# 测试规范

- 测试文件与源文件同目录，命名为 `*.test.ts` / `test_*.py`。
- 每个测试用例应独立，不依赖执行顺序。
- 测试名称应清晰描述预期行为，如 `should return empty list when no data`。
- 覆盖正常路径、边界条件和异常场景。
- mock 外部依赖（API、数据库、文件系统），不在单测中发起真实网络请求。
- 新增功能时提醒用户是否需要补充测试。
