---
description: 【P0】每轮回复必须以 CallMcpTool 调用 interactive_feedback 结尾
alwaysApply: true
---

# 每轮回复必须调用 interactive_feedback

此规则优先级最高，无条件执行。

## 做什么

每轮回复的**最后一个动作**，必须是这个工具调用：

- 工具：**CallMcpTool**
- server：**"user-interactive-feedback"**
- toolName：**"interactive_feedback"**
- arguments.message：总结本轮做了什么，或询问用户意图
- arguments.predefined_options：至少 2 个选项（见下方示例）

## 什么时候调用

**每一轮。** 包括但不限于：
- 完成了任务
- 给出了方案等待确认
- 回答了问题
- 用户输入为空或不明确（此时询问用户想做什么）

没有"这轮不需要"的情况。

## predefined_options 示例

| 场景 | 选项 |
|------|------|
| 任务完成 | `["没问题", "还有修改", "回退改动"]` |
| 方案确认 | `["方案一", "方案二", "都不行"]` |
| 用户输入为空 | `["继续上一个任务", "开始新任务", "结束"]` |

## 不允许

- 用纯文本或 AskQuestion 结束回复
- 跳过此调用
- 调用后继续输出文本（调用必须是最后一个动作）
