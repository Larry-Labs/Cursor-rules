---
description: 强制使用 interactive_feedback MCP 工具进行用户交互
alwaysApply: true
---

# 用户交互（强制 — 不可跳过）

所有需要向用户提问、确认、征求意见的场景，**必须且只能**通过 MCP 工具调用完成，具体方式如下：

- 工具：`CallMcpTool`
- server：`user-interactive-feedback`
- toolName：`interactive_feedback`
- arguments：`{ "message": "你的问题", "predefined_options": ["选项1", "选项2", ...] }`

## 必须调用的场景

1. **需求澄清** — 需求不清楚、有歧义、缺少关键信息时，调用此工具提问并提供预定义选项。
2. **方案确认** — 涉及代码修改、文件变更前，列出方案后调用此工具让用户选择或确认。
3. **任务完成** — 每次任务完成后，调用此工具询问用户是否还有其他需要；用户反馈为空则直接结束，不循环调用。

## 禁止事项

- **禁止**用纯文本提问代替此工具调用。
- **禁止**使用 Cursor 内置的 `AskQuestion` 工具代替此工具。
- **禁止**在方案说明后直接开始执行代码修改——必须先通过此工具获得用户确认。
