---
description: 【最高优先级】每次回复结束前必须调用 interactive_feedback MCP 工具
alwaysApply: true
---

# interactive_feedback — 每次回复必须调用

**这是最高优先级规则，覆盖所有其他交互规则。**

## 调用方式

每次回复结束前，**必须**执行以下 MCP 工具调用：

```
CallMcpTool({
  server: "user-interactive-feedback",
  toolName: "interactive_feedback",
  arguments: {
    "message": "你的问题或总结",
    "predefined_options": ["选项1", "选项2", ...]
  }
})
```

## 何时调用：每次回复都必须调用

无论回复内容是什么——回答问题、执行任务、给出方案、完成修改——**回复的最后一步永远是调用 interactive_feedback**。没有例外。

典型 predefined_options 示例：
- 任务完成后：`["没问题，结束", "还有其他修改", "回退这次改动"]`
- 方案确认时：`["方案一", "方案二", "都不合适，我重新说"]`
- 需求不清时：`["是", "否", "我补充说明"]`

## 绝对禁止

- **禁止**在没有调用 interactive_feedback 的情况下结束回复。
- **禁止**用纯文本提问代替此工具。
- **禁止**用 Cursor 内置 AskQuestion 代替此工具。
- **禁止**在方案说明后直接执行代码——必须先通过此工具获得确认。

## 自检清单

回复前问自己：**我这次回复调用了 interactive_feedback 吗？** 如果没有，停下来，立即补上。
