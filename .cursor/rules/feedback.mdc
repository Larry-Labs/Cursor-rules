---
description: 已废弃 — 请使用 ask-question.mdc
alwaysApply: false
---
# 每轮回复必须调用 interactive_feedback

此规则优先级最高，无条件执行。

> **⚠️ 最常见的违规场景：用户回复"没了"/"先这样"/"好的"等简短确认后，你直接用纯文本收尾而未调用 interactive_feedback。这是错误的。无论用户说什么，你的最后一个动作必须是 interactive_feedback 调用。**

## 做什么

每轮回复的**最后一个动作**，必须调用 `interactive_feedback` 工具：

- **message**：总结本轮做了什么，或询问用户意图
- **predefined_options**：至少 2 个选项（见下方示例）

工具标识：`user-interactive-feedback-interactive_feedback`（MCP 工具名因环境而异，认准 `interactive_feedback` 关键字即可）。

## 什么时候调用

**每一轮。** 包括但不限于：
- 完成了任务
- 给出了方案等待确认
- 回答了问题
- 用户输入为空或不明确（此时询问用户想做什么）

没有"这轮不需要"的情况。

## predefined_options 示例

| 场景 | 选项 |
|------|------|
| 任务完成 | `["没问题", "还有修改", "回退改动"]` |
| 方案确认 | `["方案一", "方案二", "都不行"]` |
| 用户输入为空 | `["继续上一个任务", "开始新任务", "结束"]` |

## 结束前自检（必须执行）

每轮回复即将结束时，在输出最后内容前，执行以下检查：

> **✅ 本轮回复的最后一个动作是 interactive_feedback 调用吗？**
> - 是 → 正常结束
> - 否 → **立即补上 interactive_feedback 调用，然后再结束**

这个自检没有任何豁免条件。即使你认为"这轮不需要"或"用户刚说了没了"，仍然必须调用。

## 反面示例（绝对禁止）

❌ 用户说"没了"后直接收尾：
```
用户: "没了"
助手: "好的，后续有问题随时找我。"  ← 错误！必须调用 interactive_feedback
```

❌ 以纯文本结束：
```
已完成修改，有问题随时告诉我。
```

❌ 用 AskQuestion 代替：
```
AskQuestion(...)  ← 错误，必须用 interactive_feedback
```

❌ 调用后继续输出文字：
```
interactive_feedback(...)
以上就是本次修改的总结。  ← 错误，调用后不得再输出
```

✅ 正确的结束方式：
```
（完成所有操作和说明后）
interactive_feedback(message="...", predefined_options=[...])
← 回复结束，不再输出任何内容
```
