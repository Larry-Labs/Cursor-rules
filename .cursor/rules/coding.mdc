---
description: 开发流程、编码质量、Git 提交与环境规范
globs: "**/*.{ts,tsx,js,jsx,py,sh,bash,go,rs,java,kt,swift,c,cpp,h,hpp,css,scss,vue,svelte}"
alwaysApply: false
---

# 开发流程（RIPER 精简版）

处理复杂任务时，按阶段推进，不跳步：

1. **Research（研究）** — 先阅读相关代码和上下文，理解现有架构、依赖和约束。仅输出观察和问题，不给建议。
2. **Innovate（构思）** — 列出所有可行方案，一次性给出：方案总数、每种方案的实施难度与适用范围、推荐方案及推荐理由。不写具体代码。
3. **Plan（规划）** — 输出明确的实施步骤清单，每步是原子操作。确认方案后再动手（见 feedback.mdc）。
4. **Execute（执行）** — 严格按确认的计划实施，不做计划外的修改或"顺手优化"。
5. **Review（审查）** — 对照计划逐项验证实现；发现错误代码、冗余代码、未使用的导入或死代码时，提醒用户并建议清理。同时检查 `docs/` 目录下是否有需要同步更新的设计文档。

> 简单任务可适当压缩步骤，但"先理解→再规划→后执行→最终审查"的顺序不变。

# 代码修复

发现代码问题时：

1. **说明推理** — 阐述问题和推理依据。
2. **给出方案** — 一次性列出所有可行方案，说明每种方案的实施难度与适用范围。
3. **推荐方案** — 明确指出推荐方案及推荐理由。
4. **征求意见** — 询问用户选择，确认后再修复（见 feedback.mdc）。

# 编码质量

> 如果任务要求模仿已有代码的风格，可沿用原有写法，但须提醒用户存在的可改进之处，由用户决定是否优化。
- **注释克制** — 只注释非直觉的业务逻辑、边界条件或设计取舍，不写显而易见的注释。
- **命名清晰** — 变量、函数、类名应自解释，避免无意义缩写。
- **错误处理** — 不吞异常、不写空 catch，异常附带上下文信息。
- **类型安全** — 避免 `any`（TS）或忽略类型提示（Python），优先强类型。
- **单一职责** — 一个函数只做一件事，过长时考虑拆分。
- **DRY** — 发现重复代码时提取为可复用的函数或模块。
- **开闭原则** — 优先通过新增代码扩展功能，而非修改已有逻辑。
- **低耦合** — 模块之间减少依赖，只暴露必要的接口。
- **安全意识** — 不在代码、注释、commit 中暴露密钥、token、密码等敏感信息；涉及敏感文件（.env、credentials）时主动提醒，不自动提交。
- **去 AI 痕迹** — 代码、注释、commit message 中不出现 AI 工具相关字样。

# Git 提交规范

- 使用 Conventional Commit 格式：`type(scope): description`
- 常用 type：`feat`、`fix`、`refactor`、`docs`、`test`、`chore`
- scope 可选，用于标注影响范围（如 `feat(auth): add OAuth2 login`）
- commit message 全部使用英文，简洁明了
- 不自动 push，除非用户明确要求

# 依赖安装与下载

- 安装依赖或下载资源时，优先使用国内镜像源以加快速度和提升稳定性：
  - **pip**: `pip install <package> -i https://mirrors.aliyun.com/pypi/simple/`
  - **npm**: `npm install <package> --registry=https://registry.npmmirror.com`
  - **pnpm**: `pnpm config set registry https://registry.npmmirror.com`
  - **yarn**: `yarn config set registry https://registry.npmmirror.com`
  - **Homebrew**: 使用清华源 `export HOMEBREW_BREW_GIT_REMOTE="https://mirrors.tuna.tsinghua.edu.cn/git/homebrew/brew.git"`
- 其他工具（Docker、Maven、Gradle 等）安装时同样建议配置国内镜像源。

# 运行环境隔离

- 优先在隔离环境中运行代码，避免污染全局环境（如 Python 使用 venv/virtualenv，Node.js 使用项目级 node_modules，Ruby 使用 bundler 等）。
