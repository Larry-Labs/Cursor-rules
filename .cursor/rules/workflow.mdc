---
description: Claude Code 工作流编排 — 子代理策略、任务管理、自我改进与自主修复
alwaysApply: true
---

# 子代理策略

- 大量使用子代理（Task 工具）保持主上下文窗口干净。
- 将研究、探索、并行分析工作分配给子代理，每个子代理专注一个任务。
- 复杂问题通过多个子代理并行投入算力，不要在主上下文中堆积大量搜索和阅读。

# 出错即停

- 发现方向错误或实现偏离预期时，**立即停止并重新规划**，不硬推。
- 重新规划时回到 Research/Innovate 阶段（见 coding.mdc），不跳步。

# 追求优雅

- 非琐碎改动完成后，暂停并问自己："有没有更优雅的方式？"
- 修复感觉 hacky 时，基于已知信息重新设计更干净的方案。
- 简单明显的修复跳过此步骤——不过度工程化。

# 自主修复 Bug

- 收到 Bug 报告时，直接定位并修复——不请求手把手指导。
- 主动查阅日志、错误信息、失败的测试，找到根因后解决。
- CI 测试失败时直接修复，不等用户告知如何修。

# 任务管理

- **计划先行** — 非琐碎任务（3 步以上）先将计划写入 `tasks/todo.md`，包含可勾选的清单项。
- **确认后执行** — 计划写完后通过 AskQuestion 确认再实施。
- **跟踪进度** — 完成一项勾选一项，每步提供高层级总结。
- **记录结果** — 任务完成后在 `tasks/todo.md` 中添加回顾小节。

# 自我改进循环

- 用户每一次纠正后，立即更新 `tasks/lessons.md` 记录：错误模式、根因、防止再犯的规则。
- 会话开始时回顾相关项目的 `tasks/lessons.md`，避免重蹈覆辙。
- 持续迭代经验教训，直到同类错误不再出现。
