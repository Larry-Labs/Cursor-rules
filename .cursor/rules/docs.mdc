---
description: 文档编写规范（Markdown、HTML、文本等）
globs: "**/*.{md,mdx,rst,txt,html,htm}"
alwaysApply: false
---

# 文档规范

- 使用简体中文撰写，技术术语首次出现时附英文原文（如：依赖注入（Dependency Injection））。
- 标题层级不超过 4 级，保持结构清晰。
- 重要流程、架构设计、交互逻辑等应配图说明（流程图、架构图、截图等），纯文字不足以表达时必须配图。
- 插图优先使用 SVG 或 Mermaid 语法，保证矢量清晰、可版本管理且易于修改。
- 图片统一存放在 `assets/` 或 `images/` 目录，使用相对路径引用，附 alt 文本。
- 代码示例必须可运行或标注伪代码，注明语言类型。
- 配置项、环境变量等使用表格列出，包含名称、类型、默认值、说明。
- README 包含：项目简介、快速开始、目录结构、开发命令、部署说明。
- 变更文档只记录用户可感知的改动，不记录内部重构细节。
- 不在文档中暴露真实的密钥、内网地址或个人信息，使用占位符替代。
